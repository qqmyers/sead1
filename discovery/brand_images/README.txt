This folder stores the image files we have for different brandings of the interface. 
Ultimately these will be managed elsewhere and we'll use config options to find them. 
Until then - to build a branded version, copy the appropriate files to login_img to 
overwrite the files that start with the same name stems and compile.

They are here now so they won't be part of a vanilla jar distribution.